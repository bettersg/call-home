// All this does is import the sequelize files.
// When the sequelize files are imported, sequelize immediately knows how to bootstrap the db with the exact configuration expected by the models.
require('../src/models/index.ts');
